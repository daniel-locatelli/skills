# Git skills

Skills for contributing to repositories you do not own, on any Git forge
(GitHub via `gh`, GitLab via `glab`): the discipline around pull/merge
requests and issues, independent of the language or host application the
repository targets.

Skills: `preparing-pull-request`.
