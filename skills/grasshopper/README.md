# Grasshopper skills

Skills for Grasshopper/Rhino development. Each skill pins the version-critical API facts that stale training data gets wrong and enforces a verified build–deploy–test loop against a running Rhino.
