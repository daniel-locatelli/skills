# Timber skills

Skills for timber fabrication data: the design2machine BTLx exchange format
that joinery machines consume. Each skill pins the spec facts (parameter
ranges, reference sides, schema versions) that stale training data gets wrong,
so the agent checks the reference before answering.
