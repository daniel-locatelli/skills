# Zotero skills

Skills that target a running Zotero desktop app. `using-zotero` drives the
local HTTP API (reads, adds, attachments, tags, collections) without
zotero.org. Tests: `python -m pytest skills/zotero/using-zotero/tests -v`.
